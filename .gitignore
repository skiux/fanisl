# --- secrets / 本地配置 ---
.env
**/.env
*.local
# yt-dlp 的 YouTube 登录态：内含 __Secure-3PSID 等 Google 会话凭据，绝不入库
cookies.txt
**/cookies.txt

# --- 数据库 / 数据 ---
*.db
*.sqlite
*.sqlite3
# 提帧产物（二进制、可重生成）
data_export/keyframes/
# 周报是生成物（collector 每周落盘、API 现算现返），与 keyframes 同类。
# 追踪它会让服务器每次生成都把工作区弄脏，进而挡住自动更新的 git pull。
data_export/reports/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# --- Node / 前端 ---
node_modules/
frontend/dist/
*.tsbuildinfo

# --- 编辑器 / 系统 ---
.DS_Store
.idea/
.vscode/

# --- Claude / 本地 agent 状态 ---
.claude/

# 本地开发的隔离配置，由 backend/tools/dev.sh 生成（凭据形态的文件名一律不入库）
.env.dev

# 备份出来的配置照样带口令。曾把 .env.bak-003457 提交进仓库（未推送，已改历史移除）
.env.bak*
.env.*.bak
