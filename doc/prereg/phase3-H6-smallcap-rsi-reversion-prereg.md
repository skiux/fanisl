# Phase 3 预注册：H6 — 小盘 1h RSI 极端 → 短时反转（净 PnL 确认）

> 预注册：判据/信号/切分跑前写定。写定日期：2026-06-14。
> 来由：小盘 IC 筛浮出 `rsi_1h @+4h` 前向 IC=-0.051、**18/18 标的同号**（干净、非泄漏）的短时反转候选。
> IC 显著 ≠ 扣费后赚钱。H6 是把这个候选转成**净 PnL 回测**——唯一能回答"是否可利用"的检验。

---

## A. 假设（可证伪）

**小盘永续上，1h RSI 进入极端（超买/超卖）后，未来 4h 价格朝反方向回归，扣 14bps 后净期望为正。**

- rsi_1h ≥ 70（超买）→ **做空**（fade）。
- rsi_1h ≤ 30（超卖）→ **做多**（fade）。
- 经济理由：小盘流动性薄、散户追涨杀跌制造短时过冲 → 1h 尺度均值回归。IC 筛已显示该倾向干净且 18/18 一致。
- 反面（净 ≤0）= 反转幅度 < 成本，候选作废（与"+4h 反转都输给成本"一致）。

---

## B. 信号（精确、机械、锁定，**禁止调参**）

- 特征 `rsi_1h`（1h K线 RSI，已审计为点时干净：后向相关高、前向弱）。
- 阈值 = **教科书标准 70 / 30**（非调出来的）。rsi_1h(t)≥70→short，≤30→long。
- 去重叠 4h（=持有期，样本不重叠）。分位/取值只用 ≤t 数据，进场 t 后第一根 1h bar。

---

## C. PnL / horizon

- 出场 = 进场 +4h（主），+8h/+12h 仅记录。净方向 PnL = `(ret if long else −ret) − COST`，COST=0.0014。

---

## D. universe

- 主：18 小盘篮子（H5b 同篮）。
- 对照：BTC/ETH/SOL/BNB/ZEC（看反转是小盘特有还是普适）。

---

## E. 裁决判据（锁死，①必过 + ②③④⑤；在主 universe=小盘 上）

1. `|S| ≥ 40`。
2. `mean(pnl_S) > 0` 且 bootstrap 单边 95% CI 下限 > 0。
3. `mean(pnl_S) > 随机择时零分布上限`。
4. 命中率 > 50%。
5. ≥60% 标的 mean(pnl) 为正。

→ 全过 = **第一个扣费后为正的候选** → Phase 4 前向 OOS 确认（这是 make-or-break，须真前向）。
→ ②不过 = 反转幅度 < 成本，H6 KILLED，记录"短时反转干净但不可利用"。

## F. 防自欺

1. 阈值教科书标准、跑前锁死；只测 70/30 一组，不扫阈值找显著。
2. rsi_1h 已审计点时干净（区别于被剔除的 4h/1d bar 指标）。
3. 同一 ~183d regime；若 ②过，**真确认必须前向 OOS**，in-sample 漂亮不算数。
4. IC 候选与本 PnL 检验用同一份数据，故 H6 过也只算"候选升级"，不是已落地 edge——前向才是终审。
