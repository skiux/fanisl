# Phase 3 预注册：H5b — 突破动量在小盘 universe 上是否成立（追 ZEC 线索）

> 预注册：判据/信号/切分在跑前写定。写定日期：2026-06-14。
> 这是 Phase 3 的第一刀：Phase 2 给出"现有数据无简单 edge"，但留下一条最强非随机线索——
> **ZEC（小盘高波动）在 H1（唯一正）和 H5（唯一显著正）两次冒头**。H5b 在一篮子小盘永续上检验该线索。

---

## A. 为什么（Phase 2 → Phase 3 的依据）

- H5（突破动量）在主流五币上 KILLED，但 **ZEC 是唯一显著正（+1.55%）**，去掉它合并转负。
- H1（funding 反转）里 **ZEC 也是唯一正**。两次同一个小盘标的冒头 = 目前最强的非随机信号。
- 假说：**动量 edge 不在高效的主流币，而在小盘/高波动名**（流动性溢价、套利不充分、趋势更脏更持续）。
- Phase 3 不铺广度，只验这个具体猜想：把 H5 的**锁定参数原样**搬到小盘篮子，看 ZEC 是个例还是规律。

---

## B. 信号 / PnL（**完全沿用 H5，禁止调参**）

- 触发：price 创 48h 新高→long / 新低→short（point-in-time，prior≥24 bar 且跨度≥0.8·48h），去重叠 24h。
- 进场 t 后第一根 1h bar，出场 +24h（主），扣 COST=0.0014。净方向 PnL。
- **唯一改变 = universe**：换成小盘高波动永续篮子（见 §C），其余一律不动。

---

## C. universe（小盘/高波动，回填后实际落地者为准）

候选篮子（Binance USDT 永续，避开已测的 BTC/ETH/SOL/BNB/ZEC）：
AVAX, LINK, NEAR, ATOM, FIL, APT, ARB, OP, INJ, SUI, SEI, TIA, RUNE, AAVE, LDO, DOGE, DOT, LTC。
回填 ~183d 1h price；无足够历史/无法回填者自动剔除。

---

## D. 裁决判据（跑前锁死，与 H5 同一套）

合并主 horizon +24h、去重叠 24h 的触发集 S：
1. `|S| ≥ 40`。
2. `mean(pnl_S) > 0` 且 bootstrap 单边 95% CI 下限 > 0。
3. `mean(pnl_S) > 随机择时零分布上限`。
4. 命中率 > 50%。
5. **≥60% 标的 mean(pnl) 为正**（篮子大，用比例而非固定个数；防单一肥尾带飞）。

→ 全过 = **小盘动量线索成立** → 进一步：H1/H3 也搬小盘（需回填 funding/liq/oi）+ 前向 OOS。
→ ②不过或④⑤不过 = ZEC 是个例、非小盘规律 → 记录，动量线索基本到此为止。

## E. 防自欺

1. 参数与 H5 完全一致（无 universe-specific 调参），这才是对线索的干净检验而非 p-hack。
2. 同一时间 regime（~180d）——结论仍标 SCREEN，真确认靠前向。
3. 判据④（命中>50%）对动量偏不利（动量靠偏度），但沿用 H5 锁定不改；真正看②③⑤。
4. 小盘数据质量更杂（流动性、插针），留意单标的极端值；⑤的比例判据正是为此。
