# PRODUCT — fanisl 是什么

> 前端设计的第一份文档。读完本文你应该知道：这是什么产品、给谁用、什么重要、
> 页面怎么组织、绝对不要做成什么样。接口契约见 `api.md`，领域概念见 `domain-model.md`。
> （视觉与组件规范 `design-system.md` 已于 2026-08-14 随前端重建删除，现以 `frontend/` 的实现为准。）

## 1. 一句话

**fanisl 是一个单人使用的投资研究工作台，核心是一台"持续学习、持续验证、持续沉淀
投资知识"的引擎**：持续摄取金融创作者（YouTube 财经博主）的视频内容，把其中的
判断/方法/认知提取为结构化知识，到期用市场数据机械评分，归并沉淀为可复用、可检索、
带战绩的知识库。

核心资产是**知识库本身**；验证是让知识可信的质检戳，不是产品本体。
产品的气质是**审计与研究**：每一条知识都能下钻到逐字原文证据，每一个百分比都带样本量，
每一次评分都有冻结的判据。诚实优先于好看。

## 2. 用户

一个人（owner 本人）。不是 SaaS、没有多租户、没有权限、没有"管理员"。
使用姿态是**阅读、跟踪、下钻、对照**，几乎不"录入"——所有写操作都发生在后端管线
（会话提取/CLI），前端 99% 是只读的。仅有的前端写操作：对话（发消息）、
交易评测的少量动作按钮、对话改名删除。

## 3. 四个域及其优先级

| 优先级 | 域 | 定位 | 前端体量 |
|---|---|---|---|
| **主线** | 知识引擎 | 产品本体：知识库 + 验证战绩 + 发现 | ~60% |
| 工具 | 对话（盘面助手） | 随手问盘面的 Claude 对话（带实时行情工具） | ~15% |
| 实验仪表 | 交易评测台 | 多账户对照实验的观察面板（评的是 Claude 与 playbook，不是真钱交易所） | ~20% |
| 陈列 | 研究档案 | 23 个已杀假设的收官文档，只读 markdown 陈列（负结果是资产） | ~5% |

主导航应让知识引擎占主位；交易评测与对话并列其后；研究档案放次级入口。
市场数据图表（metrics 系列端点）**不是独立的域**——它们是对话工具和知识验证的
支撑数据，不值得再建一个"数据总览仪表盘"（旧前端的败笔之一）。

## 4. 反目标（上次重写失败的直接教训，逐条禁止）

这个产品**不是**：

- ❌ **后台管理系统 / CMS**：没有"数据表格 + 筛选器 + 分页器"式的主界面；
  表格不得作为页面主布局（对照性数字列表如联赛表除外，且以精简形式出现）；
- ❌ **数据库浏览器**：不按"表"组织页面（"contents 表页""units 表页"），
  按**研究对象与任务**组织（读一期内容、看一条知识的演进、查一位信源的战绩）；
- ❌ **仪表盘堆砌**：没有一屏十二个 KPI 卡片；统计数字用一行淡色文字或小侧栏承载；
- ❌ **通用组件库风**（Ant Design Pro 式）：参照系是 Notion / Linear / Perplexity——
  阅读优先、大量留白、少边框、层级靠字号与间距；
- ❌ **假装数据很多**：库很小（见 §7），设计必须在 2 个信源 / 105 个节点下就好看好用，
  空态和小样本是常态而非异常。

## 5. 信息架构

```
fanisl
├── 知识库（默认落地页）
│   ├── 节点视图（默认）：可复用知识的列表（按提及数/状态），支持 kind/status/标签/跨源过滤
│   │   └── 节点详情：canonical 主体 + 生命周期状态 + 提及时间线（演进）+ 关系边 + 评分聚合
│   ├── 时间流：内容（视频）按发布时间的卡片流
│   │   └── 内容阅读页：知识单元流（判断/方法/认知分节）+ 折叠的原文转录与画面笔记
│   ├── 单元浏览：跨内容的判断/方法/认知三入口 + 标签枢纽 + 全文检索
│   │   └── 单元详情：主张/证据/裁决 + 价格证据图（/knowledge/prices）
│   ├── 验证中心：行动队列（即将到期/近期判定/不可判/需复核）+ 判定档案下钻
│   ├── 发现：对立命题（conflicts）+ 互补关系 + 跨源共识 + harness 候选 + 周报
│   └── 信源：联赛表（战绩/含糊率/p 值）+ 信源主页（其全部单元与节点）
├── 对话：盘面助手（SSE 流式，会话列表）
├── 评测台：账户对照 + 持仓/交易列表 + 交易详情（证据链）+ setup 评测 + 拒绝记录
└── 档案：研究收官文档陈列（markdown 渲染）
```

组织原则：

1. **节点是知识库的默认入口**（可复用知识>原始信息流）；时间流与单元浏览是证据层入口；
2. **下钻链永不断**：节点 → 提及（unit）→ 内容原文（quote 定位）→ 评分（判据+实价）——
   任何视图看到一条知识，两次点击内可到达其逐字原文和市场裁决；
3. **联赛表/周报/抽查是运营视图**：信息更新慢、密度低，放侧栏或次级页，不占主位；
4. 全局搜索（若做）走 `/knowledge/units?q=`，对象是知识单元而非页面。

## 6. 核心用户旅程（定义屏幕的六条）

1. **"这位博主到底准不准？"**
   信源联赛表（/knowledge/scoreboard）→ 信源主页 → 其 claim 列表（按到期/结果过滤）
   → 单元详情看某次判断的判据与实价。要求：命中率必带 n 与口径说明，样本小要如实。
2. **"这期视频讲了什么值得留的东西？"**
   时间流 → 内容阅读页：单元流为主体（不是转录），转录与画面笔记折叠在后。
3. **"关于 X 我们沉淀了什么知识？"**
   节点视图按标签/检索过滤（如 xauusd、ai-capex）→ 节点详情读 canonical 与提及演进。
   这是"可检索的知识库"的兑现场景。
4. **"两位信源在哪里对立？谁对了？"**
   发现页 conflicts 边 → 两侧节点详情 → 各自提及与（未来的）评分对照。
   当前只有 1 条对立边（半导体"数字地租 vs 周期涨法"）——页面为它的增长设计，
   但首屏要让这唯一一条足够醒目好读。
5. **"最近市场裁决了哪些判断？接下来一周有哪些要到期？"**
   验证中心：overview 四格 + recent 判定流（✓/✗ 带实价）+ due 即将到期列表
   → 判定档案（/knowledge/verifications/{id}）配价格证据图。
6. **"Claude 的模拟交易做得怎么样？"**
   评测台：账户切换 → scorecard 与 calibration → 交易列表 → 单笔证据链
   （计划各版本/事件/复盘全文）→ setup 评测（prior vs live 对照）→ 拒绝记录审计。

## 7. 数据现实（设计必须适配的真实规模）

截至 2026-07-18：信源 **2** 位；内容 **18** 篇（15.7 万字转录）；知识单元 **247**
（判断 135 = 3A/65B/35C/32D，方法 23，认知 89）；节点 **105**（corroborated 9）；
关系边 **6**（对立 1）；已到期评分 **62** 个时点；模拟交易 ~10 笔。

含义：
- 列表普遍 <100 项，**不需要分页**，需要的是好的分组与排序；
- 百分比样本极小（美投君已到期仅 5），**任何比率必须带 n**，联赛表要有
  "样本仍小，仅供跟踪"级别的诚实文案；
- 空态多（抽查 0 覆盖、多数节点无评分、positions 常为空）——空态文案要说明
  "为什么空、什么会让它不空"（见 design-system 文案规范）；
- 数据按天/周增长（collector 自动跑），设计要经得起 10 倍规模（信源 5-8、
  节点上千）而不需要重构 IA——这也是节点作为默认入口的原因。

## 8. 增长方向（设计余量，不是现在做）

历史回填（更多已到期战绩）、信源扩到 5-8 位（联赛表变得有对照性）、
新内容每日常态摄取（时间流变成真正的"今日"）、关系边与跨源共识增多（发现页变厚）、
K7+ 可能的：加权合成观点、假设生成、评测台与知识引擎打通。

## 9. 成功标准

打开产品 10 秒内能回答"最近有什么新知识、新裁决"；任意一条知识 2 次点击内见原文
与判据；owner 愿意每天打开它读，而不是打开数据库客户端。
