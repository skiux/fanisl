# DESIGN SYSTEM — 视觉、组件与文案规范

> 与实现无关的设计规格（值以 CSS 语义给出；如用 Tailwind，映射到最近档位即可）。
> 产品定位与反目标见 `PRODUCT.md` §4——本文假定你已接受"研究工作台，不是后台"。

## 1. 设计立场（第一次重写失败换来的，按序优先）

1. **阅读优先**：第一目标是把知识读进去，不是把数据管起来。参照 Notion / Linear /
   Perplexity 的密度与气质。
2. **一屏一个视觉重点**：每页有唯一主体（内容流 / 单元流 / 节点 canonical），
   其余一切退后。
3. **层级靠字号、字重、灰度与留白**，不靠边框和底色块。边框只允许：引文左线、
   分区 hairline（1px, 近白灰）、必要的输入控件。
4. **正文列宽 ~700px**（44rem）居中；辅助信息进右侧窄栏（sticky，≤16rem，
   xl 以上才出现）或行内淡色 meta，绝不与正文抢宽度。
5. **表格禁作页面布局**。对照性数字（联赛表、setup 对照）可用精简表格：无竖线、
   行高宽松、表头小号大写字距。
6. **克制的数字呈现**：等宽数字（tabular-nums）、样本量随行、不用大色块渲染涨跌。

## 2. 布局系统

- 页面容器：`max-width 68-76rem` 居中，水平 padding 24px；
  主列 `minmax(0, 44rem)` + 可选右栏 `13-16rem`，列距 ≥4rem。
- 垂直节奏：页顶 padding ~48px；大节间距 48-56px；节内块间距 24-36px；
  行内 meta 与主行间距 4-6px。留白宁多勿少。
- 局部滚动仅用于外层页面容器；不做嵌套滚动区（上次的 70vh 内滚框是败笔）。

## 3. 字体层级（中文为主的阶）

| 用途 | 大小/行高 | 字重 | 颜色 |
|---|---|---|---|
| 页面大标题 | 24-26px / 1.3 | 600 | 主文 |
| 阅读页标题 | 22-23px / 1.4 | 600 | 主文 |
| 节标签（如"判断 · 8"） | 12px，全大写字距 0.14em | 500 | 弱文 |
| 卡片/单元主行 | 15-15.5px / 1.5 | 500 | 主文 |
| 正文（转录/canonical/文档） | 14.5-15px / 1.9 | 400 | 次文 |
| 引文 | 13.5px / 1.7 | 400 | 弱次文 + 左侧 2px 引线 |
| meta 行 | 12-12.5px | 400 | 弱文 |
| 微标签/徽标/代码 | 10.5-11px，数字与代码用 mono | 400-600 | 按语义 |

## 4. 颜色

**中性阶**（zinc 系）：主文 #18181b · 次文 #3f3f46 · 弱次文 #71717a · 弱文 #a1a1aa ·
极弱 #d4d4d8 · hairline #f4f4f5 · 悬停底 #fafafa · 页面底 #ffffff。
浅色为主题；暗色可后置，但选色时避免写死无法反转的组合。

**语义色**（唯一允许的彩色，全部用"浅底+深字"徽标形态，不做大面积色块）：

| 语义 | 色族 | 用途 |
|---|---|---|
| hit / verified / 正向 | emerald（底 #ecfdf5 字 #059669） | 评分✓、已验证状态、A 级 |
| partial / 提醒 | amber（底 #fffbeb 字 #d97706） | 评分½、C 级（带条件）、contested 可再议处 |
| miss / contested / 风险 | rose（底 #fff1f2 字 #e11d48） | 评分✗、存在争议 |
| 中性/不可判 | zinc（底 #f4f4f5 字 #71717a） | 条件未触发、无价格、D 级、retired |

分级字色（无底）：A=emerald-600 · B=zinc-500 · C=amber-600 · D=zinc-400。
涨跌箭头（↑↓）用字符不用色块；价格数字不红绿染色（这不是行情软件）。

## 5. 组件清单（规格要点）

- **OutcomeBadge**：`{日期缩写 + ✓/½/✗ 或中文短标}`，mono 10.5px，浅底圆角 4px；
  tooltip 给 horizon_label、outcome 全称、realized 关键数（eval_close 等）。
- **GradeMark**：单字母 A-D，mono 600，按 §4 字色；tooltip 给分级含义（domain-model §2）。
- **StatusBadge**（节点状态）：中文标签，浅底徽标，色按语义表。
- **QuoteBlock**：左 2px 引线 + 13.5px 弱次文；行首 mono 小号时间戳 `[MM:SS]`
  可点（跳原文/原视频 `url&t=秒`）。
- **UnitBlock**（单元的标准渲染，全站复用）：三段式——主张行（claim: 标的+方向箭头+
  目标/区间+至日期；method: name；concept: canonical_statement）→ meta 行（分级/期限/
  参考价/承诺度/tags）→ QuoteBlock；claim 追加条件行与"评分口径：success_def"淡色小字。
- **NodeCard**：title（15.5px/500）+ status 徽标 + canonical 两行截断 +
  meta（提及×n · 信源×n · 时间跨度 · 评分聚合微章）。
- **AttestationTimeline**：按发布时间的竖列；每项=日期 + relation 徽标（supersedes/
  contradicts 必须醒目）+ 信源 + QuoteBlock + note；演进链是节点详情的主叙事。
- **PriceEvidenceChart**（判定档案/单元详情用）：daily_bars 收盘线 + 标注层：
  ref_price 虚线、判界（target/low/high）横带、eval_ladder 时点竖标（染 outcome 色）。
  小尺寸（高 ~200px）、无网格线或极淡、坐标字 mono 10px。
- **StatLine**：一行淡色统计文字（"2 位信源 · 18 篇 · 247 个单元"），代替 KPI 卡阵。
- **LeagueTable**：精简表格（见 §1.5），每行：信源名 / 命中率+n / 含糊率 / p（tooltip 口径）。
- **EmptyState / ErrorState / Skeleton**：见 §6。
- **Markdown 渲染器**：周报、研究档案、对话回复共用；标题降两级适配容器。

## 6. 四态（每个取数视图必须显式处理）

- **加载**：骨架块（灰条，圆角，宽度错落），不闪 spinner；
- **空**：一句"为什么空"+一句"什么会让它不空"（例：抽查——"还没有抽查记录。
  运营流程每周从单元中随机抽样人工核对忠实度。"）；禁止孤零零的"暂无数据"；
- **错误**：中文原因（后端 detail 直传）+ 重试按钮；`/price` 的单项 error 用行内
  占位（"—"+tooltip），不整屏报错；
- **部分**：SSE 流式增量渲染；status 事件显示"正在查询 <工具名>"弱文案。

## 7. 文案规范

- 枚举中文标签以 `domain-model.md` §4 为唯一来源，组件内不得另译；
- 百分比必带样本（"命中率 42%（51）"）；n<10 时加"样本仍小"级别说明；
  固定免责句："评分为到期机械执行（hit=1，partial=0.5）。样本仍小，仅供跟踪。"
- 日期：列表用 `M 月 D 日`，详情用 `YYYY 年 M 月 D 日`，评分时点用 ISO `MM-DD` mono；
- 语气：审计式克制——陈述判据与结果，不评价（"未中"而非"翻车"）；不用感叹号；
  自称战绩类字段必须带"（自称，不采信）"；
- 中英混排：中文与拉丁字符间留一个空格；标的代码全大写 mono。

## 8. 交互与数据行为

- 轮询：价格条 10s；知识域列表 60s；评测台 30s；详情页不轮询（进入时取）；
- 路由即状态：过滤条件进 URL query（可分享/可后退）；详情页可直链；
- SSE：fetch + ReadableStream 解析（协议见 api.md §2），Esc 中断；
- 点击目标 ≥32px 高；hover 才出现的信息仅限次要补充（tooltip 口径类）；
- 无破坏性操作确认弹窗泛滥——全站仅对话删除、撤单需要确认（原生 confirm 即可）。
