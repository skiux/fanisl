# knowledge — active

知识引擎（`backend/fanisl/knowledge/` + `assets.py` + 语料）。
席位说明见 `backend/fanisl/knowledge/AGENTS.md`。最后核对 2026-09-24。

## Now
用户 2026-09-24 说「按你建议的来」，按下面顺序做，每步验证后单独提交：

1. ~~推送 c125–c128 那批~~ —— 用户已推，服务器在新代码上（2026-09-24 核）
2. ~~评分作废（留痕）~~ —— `review void`，#314 的 8-29、#937 的 5-12 与 7-11 已作废（作废记录 #1–#3）
3. ~~规范升 v3 并按 v3 重判核查 #1 那 4 条~~ —— 见下面第 3 条
4. **体检命令**：一条命令报出未登记标的、漏填 asset_symbol、评分器解析不了的单元、到期未评的时点
5. **归并积压 504 条**（第 9 条）：分批做，每批报进度

## Next
- 第 1 条剩下的登记缺口（LULU 等）
- 第 2 条抽查欠账（历史存量）
- Andy 回填 25 期：用户 2026-09-10 说暂不做

## Blocked on
- 无

## Requests out
- **base**：`api.md` §5.0 的 claim payload 补 v3 的三处新增（`grade_note`、按阶梯日给值的 magnitude、
  scoring_spec 的机器字段）；另有 console 新增的两条接口没写进 `api.md`（`test_api_doc` 两条失败）
- **frontend**：显示 `grade_note`（定级说明），magnitude 按阶梯日给值时取对应的值

## Requests in
- **frontend 席位**：标的页为空多半是标的未登记（见下面第 1 条），不是前端问题
- **frontend 席位（2026-09-14）**：单元详情与判定档案的「标的」只显示 `asset_symbol`，`asset_text` 以
  「标的说明」另起一段完整显示。v3 起理由写进 `grade_note`，`asset_text` 回到原文表述

---

> 这些原本只活在会话记录里，被压缩掉两次；现在放这儿。

## 1. 标的登记缺口

当前 active claim 里 `asset_symbol` 未登记的：

- **LULU** —— 16 条单元带 `lulu` 标签，因未登记而在标的页上不可见
- **DFEDTARU** —— FRED 序列，已在 `assets.py` 以 rate 类登记（与 T10Y2Y、T10YIE 同）

另有一批只出现在 D 级 claim 的 `asset_text` 里、没填 symbol 的：
PICK、IWD、USMV、GUNR、URA、SETM、CRDO、PANW、DELL、XLP。登记之后这些 claim 才可能升到 D 以上。

2026-09-23：COST、KO、PG、VIK、ADBE、ARM 与 FRED 的 T10YIE 已登记并回填日线；D 级 claim 漏填
asset_symbol 而标的已登记的单一标的 7 条已补（#1188 #1203 #1218 #1219 #519 #520 #565；修改记录 #6–#12），
其余 15 条是多标的并列或标的本身未登记，保持为空。`import_units` 的 `check_vocabulary` 每次导入都会报出
未登记符号——有意露出的信号，不要静音。

## 2. 抽查欠账

§10 要求每批抽满 20%，v3 起 A/B/C 全取。**按批次是结清的**（c125–c128 15/75），但历史存量欠着：
累计 155/1764 = **8.8%**，v1 时期那 798 条尤其稀。

## 3. 规范 v3（2026-09-24 已改，`pending-v3`）

改动清单见 `extraction-guide.md` §12：quote 自足（§0.1）、定级说明写 `grade_note`（§2）、期限词先判修饰
对象（§3）、机器判据写进 `scoring_spec` 并在导入时验可解析性（§4）、range_hold 的 magnitude 只写判据用到
的边（§4）、幅度词映射表（§4.1，阈值为 2025-09 至 2026-09 实测的同类 1σ）、参考价取作者说话前的收盘并在
导入时自动补（§5）、分层抽查（§10）。v1/v2 不重提。

**按 v3 重判核查 #1 列出的 4 条**（修改记录 #21–#24）：#1518 取作者 c116 盘前给的 2%，起点 9-10，D→B；
#1596「下行空间非常有限」取宽基 1σ（2%/4%/6%），D→B；#1281「下行风险相对有限」+「短期」取大盘个股档 3%，
带不可判定的例外，D→C；#57 维持 D（幅度词在评价标准里，「最近」不在期限表）。阈值按表与原话定，没有看结果。

**回看往期**（2026-09-23 起每批执行，已写进 knowledge/AGENTS.md）：新内容回指往期时，按同作者、同标的找出
旧单元逐条归类——(a) 读错原文 → `review amend`；(b) 结果可知之前补充了口径 → 可用于旧单元的判据，注明出处；
(c) 改了立场 → 新单元；(d) 事后复盘 → 不提取，但要触发核对旧单元。c125–c128 这批的结果：#1218 等 7 条补
asset_symbol，#1736 升 B；c127 称七月初的展望「几乎全对」，而 8-11 的更新 #451「纳指再回调 5%-7%」至今最低
只到约 -2.9%，单元按当时原话记。

## 4. Andy 回填 25 期

2025-12-29 ~ 2026-03-25，未提取。用户 2026-09-10 说过「回填的暂时先不管」。

## 5. 机械检查

有机械校验的地方质量高（quote ∈ 原文、v3 的判据可解析性，不过就整文件拒绝），没有的地方一定会漂。

1. **体检命令**（Now 第 4 步）：未登记的 asset_symbol 与资产标签、漏填 asset_symbol、A/B/C 判据评分器
   解析不了（含存量靠覆盖表的）、到期未评的时点。导入时的警告只响一次、没人汇总，所以能攒到十几个
2. **文档数字断言** —— 符号数、覆盖表条数这类数写进测试，代码变了文档不改就红
3. **quote 自足性** —— v3 §0.1 已写成规则；机械检查（期限词、价位是否在 quote 内）还没做

## 6. 评分器：一条坏单元会让之后全部停评（已修）

- 8-29 至 9-23 评分器实际停评：8 条 range_hold 的机器配置与 success_def 不一致，一条抛错，id 更大的
  全部没有新评分，每日任务只记一行日志。2026-09-23：`scorers.run()` 逐个时点隔离、评完再抛失败清单；
  8 条补了配置；补评 78 个时点。
- 2026-09-24：v3 把机器判据挪进 `scoring_spec`，导入时 `spec_problems` 就验，不再等到期那天。
- 错误阶梯的评分可以作废了（`review void`，整行留在 `claim_score_voids`，统计不再计；作废过的单元评分
  字段仍锁定）。

## 7. `asset_text` 被当成理由栏（v3 已给理由单独的去处）

v2 把定级理由塞进 `asset_text`（全库 v2 平均 51 字），前端把它显示成「标的」。v3 起理由写 `grade_note`，
`asset_text` 只写原文表述；前端显示 `grade_note` 的请求已发。v2 存量不回改。

## 8. 参考价口径（v3 已定）

v1/v2 缺参考价时评分器回填**发布日**收盘，对收盘后或周末录制的信源晚一个交易日（c127 周日录、周一发，
周一的变动进了起点）。v3 §5：取作者说话前最近的收盘——发布时刻按 12:00 UTC 计，美股、商品、加密、FRED
取前一交易日，韩股取当日；导入时自动补。v1/v2 存量不改。

## 9. 归并（K5）积压

最后一次归并是 2026-08-19。之后提取的 c67–c128 里，method/concept 共 504 条没有挂到任何节点
（2026-09-23 实测，#1086–#1815），节点层的「重申 / 跨信源印证」计数因此停在 8 月中。流程见
`README.md`「日常运转」：`nodes export` → 按 merge-guide 逐条判 → `nodes import` → 最后才
`seed-singletons --commit`（顺序不能反，种单例不可逆）。Now 第 5 步，分批做。
