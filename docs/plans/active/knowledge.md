# knowledge — active

知识引擎（`backend/fanisl/knowledge/` + `assets.py` + 语料）。
席位说明见 `backend/fanisl/knowledge/AGENTS.md`。最后核对 2026-09-13。

## Now
（空）c118–c124 七期已提取并结清抽查（2026-09-17）。

## Next
按下面编号排列。**第 6 条的结构问题建议最先做**——一条坏单元能让之后的全部 claim 停评。

## Blocked on
- **等用户定：幅度词阈值**（核查 #1，单元 #1518）。已以 needs_info 答复，提议见下面第 3 条第三项；
  用户回复后改规范、重判 #57 #1281 #1518 #1596 并挂修改记录

## Requests in
- **frontend 席位**：标的页为空多半是标的未登记（见下面第 1 条），不是前端问题
- **frontend 席位（2026-09-14）**：第 7 条前端那一半已做——单元详情与判定档案的「标的」只显示
  `asset_symbol`（没有时写"未规范化"），`asset_text` 以「标的说明」另起一段完整显示，与 symbol 相同时不显示。
  理由迁走之后这一段会自然变回原文表述，前端不用再改。单元核查的站上入口也已完成（夹具验收），
  等 base 推送上线

---

> 这些原本只活在会话记录里，被压缩掉两次；现在放这儿。

## 1. 标的登记缺口（可立刻做）

`assets.py` 归属明确之前被冻了一周多。当前 active claim 里 `asset_symbol`
未登记的：

- **LULU** —— 16 条单元带 `lulu` 标签，因未登记而在标的页上不可见
- **DFEDTARU** —— 联邦基金目标利率上沿，是 FRED 序列不是标的，
  归 `data/fred_source.py` 的 `FRED_SERIES` 管。**需要确认它该不该进 assets.py**，
  两条 A 级 claim（c2 #16、c19 #293）与 c114 #1502 都用它当 `asset_symbol`

另有一批只出现在 D 级 claim 的 `asset_text` 里、没填 symbol 的：
PICK、IWD、USMV、GUNR、URA、SETM、CRDO、PANW、DELL、XLP。
登记之后这些 claim 才可能升到 D 以上。

`import_units` 的 `check_vocabulary` 每次导入都会把未登记符号报出来——
这个警告是有意露出的信号，不要静音。

## 2. 抽查欠账

§10 要求每批提取抽满 20%。**按批次是结清的**（最近四批分别 21%、21%、20.9%、20.6%），
但历史存量欠着：累计 110/1542 = **7.1%**，v1 时期那 798 条尤其稀。

## 3. 待补的规范条款

都要升 `extractor_version`，所以攒在一起改：

- **§10 应写明分层抽查**：带 spec 的 A/B/C claim 要全取或按比例保证覆盖。
  纯随机抽样大概率漏掉它们，而 §11 里 v1 的七条教训有六条是分层抽查翻出来的。
- **§0.1 应要求 quote 自足**：现有条文只禁「添加原文没有的推论」，
  但反复出现的失误是另一种——canonical / success_def 的依据落在 quote 之外的
  相邻句。事实在原文里，问题是仲裁时指不出来。连续三批抽查都撞到这一条
  （#1497、#1502、#1511、#1525、#1361、#1429、#1442、#1445、#1458）。
  #1502 那次尤其要紧：支撑 A 级定级的期限词不在 quote 里。
  2026-09-17 那批又撞到三次（#1600、#1597、#1684），其中 #1600 与 #1502 完全同型——
  A 级的期限（下周议息）不在 quote 里。已用 `review amend` 修掉并留痕（unit_amendments #1-#3）。
  **在规范改之前，每批自查时把『A/B/C 的期限词与价位是否都在 quote 内』当作必查项。**
- **幅度词映射表**（2026-09-17，核查 #1 暴露）：§3 冻结了模糊期限词，幅度词（有限、小幅、
  大幅）没有，按 §0.6 只能判 D。#1518「下跌的幅度也是有限的」立场、标的、事件期限都明确，
  缺的只是阈值。提议：作者在结果可知之前自己给过数的用作者的数（c116 #1540「周五跌 2%」
  即全仓出手），否则按资产类别与期限分档的表；有了阈值这类可以判 B。起点取事件前一天收盘
  （`baseline_date`）。初筛同类：#57 #1281 #1518 #1596（D 级里 quote 含幅度词 8 条，其余 4 条
  不是幅度判断）。**阈值待用户定。**
- **回看往期**（同上）：提取是逐期做的，后续内容回指往期时没有回头核对旧单元。实测
  已提取 95 篇里 50 篇含往期回指（正则粗筛「上期 / 那期视频 / 周四…说过 / 反复强调」等，
  共 79 处；TALK君 24/42、Andy 21/37、美投君 5/16）。#1518 就是例子：c116 给了作者口径，
  c117 作者回头讲了这条，两期提取时都没看 #1518，c117 的 #1596 又判成同样的 D。
  要加的一步：新内容回指往期时，按同作者、同标的找出旧单元逐条归类——
  (a) 暴露了旧单元读错原文 → `review amend`，理由写明依据的原文；
  (b) 结果可知之前补充了口径 → 可用于旧单元的判据，注明出处；
  (c) 改了立场 → 新单元，不改旧单元；
  (d) 事后复盘自己的判断 → 不提取（§0.4），但要触发核对旧单元。
  已评分的单元评分字段锁定，(a)(b) 也改不了评分，只能留痕。

## 4. Andy 回填 25 期

2025-12-29 ~ 2026-03-25，未提取。用户 2026-09-10 说过「回填的暂时先不管」。

## 5. 三条机械检查（真正动质量的一步）

目录重构解决归属与可发现性，不解决准确。有机械校验的地方质量高
（quote ∈ 原文，不过就整文件拒绝），没有的地方一定会漂。要加的：

1. **标的登记检查** —— active 单元里出现的 `asset_symbol` 与资产标签必须已登记。
   现在只在导入时警告一次、没人汇总，所以能攒到十几个
2. **文档数字断言** —— `90 符号`、`77 条 overrides` 这类数写进测试，
   代码变了文档不改就红。设计文档里曾长期写着 39 和 103
3. **quote 自足性** —— 见上面第 3 条，需先改规范

## 6. 评分器：一条坏单元会让之后全部停评

2026-09-13 发现。触发点已修，结构问题没修。

- **#1264（c104，C 级，12-31 到期）原本评不了分。** `success_def` 写「上限 4.25%」，
  `magnitude` 却只有 `{max_hikes: 2}`，`range_hold` 读不到上界。用 09-11 的真实行情调
  `score_unit_at` 实测，抛 `ValueError: 双边 magnitude 需显式 bounds override`。
  已在 `scoring_overrides.json` 补 `level_high: 4.25`——语义不变，只是让机器配置对齐
  已冻结的 `success_def`；复测判 hit。
- **结构问题（未修）：** `scorers.run()` 只有外层 `try/finally`，没有逐条的异常隔离。
  单元按 id 升序评，#1264 一旦到期就每天重试、每天抛错，id 比它大的所有 claim 从那天起
  全部停评，除了日志里一行异常没有别的迹象。修法：循环体内逐条 `try/except`，
  失败记成一条结果并继续。
- **同类预防：** A/B/C 的 spec 能不能被评分器解析，应该在导入时就验，而不是到期那天才发现。

## 7. `asset_text` 被当成理由栏，前端把它显示成「标的」

- `models.py` 对它的定义是「原文的资产表述」，v2 实际把定级理由都塞了进去。
  实测（active claim）：全库 v2 共 253 条，平均 51 字、超 40 字 133 条、含 § 编号 12 条；
  c113-c115 平均 101 字。c116-c117 去掉了 § 编号，但仍平均 58 字——**长度问题没解决**。
- 根因：D 级没有 `scoring_spec`，也就没有 `success_def` 可放「为什么判 D」，理由只能挤进
  `asset_text`。
- 前端 `EvidenceDossier` 与 `VerificationDossier` 的「标的」字段取
  `asset_text ?? asset_symbol`，所以单元详情里「标的」一栏显示整段推理（用户 2026-09-13
  截图 #1518）。
- 要两个席位一起定：知识侧给理由一个单独的去处，前端侧「标的」显示 `asset_symbol`、
  理由另起一行。已在 `frontend.md` 的 Requests in 留言。
