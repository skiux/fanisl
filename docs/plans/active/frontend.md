# frontend — active

知识引擎站点（`frontend/`）。席位说明见 `frontend/AGENTS.md`。

## Now
- **验证页重做**（2026-09-18，用户提的三点：滚动太深、信息太密、说明文字多余）：**整页不滚动**。上面一条判决时间轴
  （以今天为界，命中在上、未中在下，未到期压在轴上），下面一屏卡片是轴上一段的放大镜，轴上高亮标出这一屏的日期，
  可翻「更早 / 更晚」、点轴、键盘或拖动；记录在浮层里看。图例兼作筛选。去掉大标题区、分类说明、「接下来要验证」条、
  页脚标语与旧的独立判定档案页（两栏各自内滚）。经过三版：单栏无限列表 + 原位展开（仍是深度滚动，被否）、
  按屏翻页的列表（被评"敷衍、没有设计感"）、现在这版。用户明确说不参照标的页的布局。
  真实数据实测：1440×900 / 1280×720 / 1024×768 / 390×844 整页可滚动距离都是 0，一屏 9 / 6 / 6 / 3 张卡片；
  最长的 5 条记录在桌面浮层里放得下，手机上浮层内仍要滚 97–157px。已推送（2026-09-18）
- **卡片减负与全站样式统一**（2026-09-23，用户：卡片信息太密、样式一般；多个页面颜色样式不统一）。已提交为 `2628888`，2026-09-24 随 knowledge 席位的推送（`6c603f4`）一起上线：
  - 卡片只留结果标签、标的、日期、原话、信源，原话改衬线体，按卡片高度露行；1440×900 一屏 6 张（原 9 张）
  - 实测的不统一：904 种颜色字面量（文字灰 372 种）、7 种页标题字号（30–72px）、6 种内容宽度（1200–1500px）、
    5 套页面底色、一半页面的等宽字在 Chrome 里是 Courier、「部分命中」在标的页是紫在单元详情是黄
  - 改法：`index.css` 定一套变量与共用页头 / 分段按钮 / 统计 / 筛选片 / 搜索框 / 按钮；五页换用，页头统一成
    「标题 + 视图切换 + 统计或搜索」，去掉页头的英文眉标、口号、说明段与页脚标语；文字灰收成 4 级；
    删掉组件已不再引用的旧样式约 2800 行。改后颜色字面量 521 种、文字灰 41 种，五页标题同为 30px、同一起点、与顶栏同宽
  - 09-24 用户定"全部去掉、尽量极简"：全站的英文标签（约 80 处，含首页插图注记）、分区标题下的说明句、档案侧栏的
    原则与研究顺序两段、页底统计条、应用页的纸面纹理图都去掉；中文标签不再加宽字距；空态与报错只留一句话加按钮。
    发现页的检索与"对立 / 连接簇"换成全站的搜索框与筛选片
  - 真实数据：09-24 隧道恢复后五页都用真实数据看过（1440×900）。顺手改了三处：发现页剩下的两个英文「VS」换成「↔」；
    单元详情「期限」把 `open_ended` / `within_duration` 原样印出（实测 331 + 192 条，天数是数字、原代码按字符串取不到），
    改成「原文未给期限」「发布后 N 天」；标的页未到期判断的幅度原来印英文键名（`pct 2`、`pe_low 20`），改成「下界 / 上界 / 目标」
    与百分比，其余临时键名不单列（判据里有中文写法）
  - 首页六幕的正文左边距从 7vw 改为与顶栏左缘对齐（`--site-gutter`）：1024 / 1440 / 1920 下六幕标题的左缘都等于顶栏左缘
    （改前 1440 下差 64px、1920 下差 165px）；右边距不动，给章节导航留位置
- **加载慢的排查**（2026-09-24，用户问知识页、验证页加载久是前端还是后端）：主要在网络与后端配置，不在查询，也不主要在前端
  - 数据库：`verification-page` 的两条查询在服务器上执行 2–4ms，不是瓶颈
  - 线上实测：到服务器往返约 0.35s、下载 230–390KB/s；**nginx 只压缩了 index.html**，API 的 JSON 与 240KB 的主 JS 都原样传。
    验证页一次要取约 890KB JSON。请求已写进 `base.md`（开 gzip，含配置）
  - 前端改了一处：知识库原始内容首屏不再等 500 多个节点（约 420KB），节点在内容之后取。按线上条件用真实返回回放，
    原始内容首屏 4.0s → 1.9s；验证页 6.4s 不变。gzip 上线后回放估计：验证页 2.5s、原始内容 1.4s
  - 试过"记住总数、一开始就把所有页并发发出"省一轮往返，回放下验证页反而 6.4s → 7.4s（带宽被抢），没采用
- **单元核查前端**（`features/unit-review.md` 第 5 节）：单元详情的「核查」tab 与顶栏「待确认」入口
  已完成，写接口全部在 `e2e/api-fixture.ts` 里按 `api.md` §5.6 的状态机验过。**未对真接口联调**：
  本机 API 连的是生产隧道，写接口不能在本机点；base 这批已推送。由用户在站上对一条真实
  单元提交（交接表第 4 步），前端这边再看一次真数据下的样子

## Next
- **等用户定**：标的页的页头与其余四页不一致——其余四页标题在左上角、是名词（知识库、验证、发现、档案），标的页左侧是
  通栏的标的列表，标题在列表右边、是一句话（「先看还有什么没兑现」）。对齐要改标的页的布局，用户说过标的页布局更糟，未动
- 首页 1024 宽时第 3 幕的节点卡片与右侧章节导航叠在一起（卡片右边距 `clamp(50px, 8vw, 128px)`），改左边距之前就是这样
- lint 剩 29 条 warning（2026-09-13 的 HEAD 为 43，09-18 验证页重做后从 34 降到 29），都是 React Compiler 一类的提示（effect 里同步
  setState 等），不影响运行。改到哪个页面顺手清哪个，不单独开一轮

## Blocked on
- ~~knowledge：`/knowledge/relations` 带两侧节点计数~~ —— knowledge 席位 2026-09-25 已实现（随 main 上线）：每条边新增
  `a_hit a_partial a_miss a_n_creators a_n_contents`，b 侧同名，口径同 `/knowledge/nodes` 的行。发现页简报可以改成只用关系边一次请求
- **用户（登服务器）**：gzip 已由 base 写进仓库的 `deploy/nginx-fanisl.conf`，但线上生效的配置被 certbot 改过、与仓库
  不同步，要按 `deploy/README.md`「压缩（生效配置里补 gzip）」在服务器上补上并 reload。这是验证页首屏的主要瓶颈

## Requests in
- **knowledge 席位（2026-09-24）**：内容状态新增 `reference`——只入库供阅读、不做提取（@MeiTouNews 每日新闻，用户定
  重点在新闻与学习价值）。现在单条内容页会直接印出英文 `reference`。请在 `docs/DOMAIN.md` 的 content.status 与
  `shared/domain/labels.ts` 同时加 `reference=仅阅读`（`labels.test.ts` 对着 DOMAIN 核，两处要一起改）
已处理（2026-09-24）：
- base：gzip、relations 计数与两份无主文档的回复已看，Blocked on 按此改写
- knowledge（提取规范 v3）：① `grade_note` 在单元详情「冻结判据」与验证浮层「更多判据信息」里紧跟「标的说明」显示为
  「定级说明」，没有就不显示；浮层里这两项是整段话，改为占一整行、排在短项之后。核查记录的字段名也补了「定级说明」。
  真实数据 4 条有此字段（#57、#1281、#1518、#1596），#1596 已在两处看过。② 分档判界：`features/asset/format.ts` 新增
  `magnitudeThresholds(magnitude, day)`，按阶梯日取那一档（没有正好那天的取之前最近一档）；验证浮层的价格图与页头、
  标的页总览的到期列表、标的档案的未到期判断都改用它。#1596 实测：09/19 画「下界 7503.84」，标的页 10/12、12/11 两个时点分别是
  7350.7、7197.56（改前这两处不显示判界）。夹具加了一条分档 + 定级说明的记录，`verification.spec.ts` 加了对应用例
已处理（2026-09-14）：
- knowledge：单元详情「标的」改显示 `asset_symbol`，`asset_text` 以「标的说明」另起一段完整显示。
  请求点名的两处之外，判定档案待执行视图与内容页的单元标题行也改了
- knowledge：核查 tab 与待确认入口，见 Now
- base：`frontend/AGENTS.md` 的归属说明已改为"接口变更请求写进 `base.md`"
- **已处理（2026-09-17，用户让 knowledge 席位直接改）** knowledge 席位：知识站上去掉按角色区分的地方，所有登录用户一视同仁
  （根 `AGENTS.md` §1，用户定的原则）。实测四处：`UnitReviews.tsx` 的 `canWrite`、
  `frontend/src/shared/navigation/ReviewInbox.tsx` 的 `isAdmin`、`frontend/src/shared/auth/AccountMenu.tsx` 的角色标签与
  「用户管理」入口（用户管理在 console 里）、`reviews.ts` 的注释。接口侧由 base 同步放开；
  与 base 的改动同一个提交。另改了 `e2e/api-fixture.ts`（去掉 403 分支）、`e2e/unit-review.spec.ts`（全部以成员身份跑，
  403 用例换成 409）、`e2e/auth.spec.ts`（以管理员登录也不显示角色）。验收见 `features/unit-review.md` 第 6 节 3c
